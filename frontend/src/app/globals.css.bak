<<<<<<< Updated upstream
@import "tailwindcss";
=======
@import url('https://fonts.googleapis.com/css2?family=Outfit:wght@100;300;400;500;700;900&display=swap');

:root {
  /* PBST Brand Palette */
  --pbis-primary: #1e3a8a;    /* Deep Professional Blue */
  --pbis-secondary: #3b82f6;  /* Action Blue */
  --pbis-accent: #6366f1;     /* Educational Purple */
  
  /* Tiers & Status */
  --tier-1: #10b981;          /* Success/Core */
  --tier-2: #f59e0b;          /* Warning/Targeted */
  --tier-3: #ef4444;          /* Danger/Intensive */

  /* Surface & Backgrounds */
  --bg-deep: #020617;
  --bg-surface: rgba(15, 23, 42, 0.6);
  --bg-card: rgba(30, 41, 59, 0.4);
  
  /* Text */
  --text-primary: #f8fafc;
  --text-secondary: #94a3b8;
  --text-muted: #64748b;
  
  /* Accents & Glass */
  --accent-blue: #3b82f6;
  --accent-purple: #8b5cf6;
  --accent-emerald: #10b981;
  --accent-rose: #f43f5e;
  
  --glass-border: rgba(255, 255, 255, 0.08);
  --glass-border-bright: rgba(255, 255, 255, 0.15);
  
  /* Premium Extras */
  --gradient-pbst: linear-gradient(135deg, #1e3a8a, #3b82f6, #6366f1);
  --gradient-premium: linear-gradient(135deg, #3b82f6, #8b5cf6, #10b981);
  --shadow-premium: 0 20px 50px rgba(0, 0, 0, 0.5);
  --shadow-subtle: 0 10px 30px rgba(0, 0, 0, 0.3);
  
  --font-main: 'Outfit', 'Inter', -apple-system, BlinkMacSystemFont, "Apple SD Gothic Neo", sans-serif;
}

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: var(--font-main);
  background-color: var(--bg-deep);
  color: var(--text-primary);
  overflow-x: hidden;
  min-height: 100vh;
  line-height: 1.5;
  -webkit-font-smoothing: antialiased;
}

/* Base Layout */
.app-container {
  max-width: 1400px;
  margin: 0 auto;
  padding: 120px 40px 60px;
}

/* Glassmorphism */
.glass {
  background: var(--bg-surface);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border: 1px solid var(--glass-border);
  border-radius: 32px;
}

.glass-interactive {
  background: var(--bg-surface);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border: 1px solid var(--glass-border);
  border-radius: 32px;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
  cursor: pointer;
}

.glass-interactive:hover {
  transform: translateY(-8px) scale(1.02);
  border-color: var(--glass-border-bright);
  background: rgba(30, 41, 59, 0.8);
  box-shadow: var(--shadow-premium);
}

/* Gradients */
.gradient-text {
  background: var(--gradient-premium);
  -webkit-background-clip: text;
  background-clip: text;
  -webkit-text-fill-color: transparent;
  display: inline-block;
}

.gradient-bg-blue {
  background: linear-gradient(135deg, rgba(59, 130, 246, 0.15), transparent);
}

/* Typography Utilities */
.text-xs { font-size: 11px; }
.text-sm { font-size: 14px; }
.text-lg { font-size: 18px; }
.text-xl { font-size: 20px; }
.text-2xl { font-size: 24px; }
.text-5xl { font-size: 48px; }
.font-black { font-weight: 900; }
.font-bold { font-weight: 700; }
.uppercase { text-transform: uppercase; }
.tracking-tight { letter-spacing: -0.02em; }
.tracking-widest { letter-spacing: 0.1em; }

/* Dashboard Specific Grid */
.dashboard-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 32px;
  margin-bottom: 60px;
}

.student-list-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 24px;
}

/* Component Styles */
.stat-card {
  padding: 40px;
  display: flex;
  flex-direction: column;
  gap: 16px;
  position: relative;
  overflow: hidden;
}

.stat-card::before {
  content: "";
  position: absolute;
  top: 0;
  right: 0;
  width: 150px;
  height: 150px;
  background: radial-gradient(circle, var(--accent-blue) 0%, transparent 70%);
  opacity: 0.05;
  filter: blur(40px);
}

.progress-bar-container {
  height: 12px;
  background: rgba(0, 0, 0, 0.3);
  border-radius: 100px;
  overflow: hidden;
  border: 1px solid rgba(255, 255, 255, 0.05);
}

.progress-bar-fill {
  height: 100%;
  background: var(--gradient-premium);
  border-radius: 100px;
  transition: width 1s ease-out;
}

.tag {
  padding: 6px 14px;
  border-radius: 12px;
  font-size: 11px;
  font-weight: 900;
  background: rgba(255, 255, 255, 0.05);
  border: 1px solid rgba(255, 255, 255, 0.08);
  color: var(--text-secondary);
}

.tag-blue {
  background: rgba(59, 130, 246, 0.1);
  color: var(--accent-blue);
  border-color: rgba(59, 130, 246, 0.2);
}

/* Animations */
@keyframes fadeIn {
  from { opacity: 0; transform: translateY(20px); }
  to { opacity: 1; transform: translateY(0); }
}

.animate-fade-in {
  animation: fadeIn 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards;
}

@keyframes shimmer {
  0% { transform: translateX(-100%); }
  100% { transform: translateX(100%); }
}

.animate-shimmer {
  animation: shimmer 2s infinite;
}

/* Responsive */
@media (max-width: 1200px) {
  .dashboard-grid { grid-template-columns: repeat(2, 1fr); }
}

@media (max-width: 768px) {
  .dashboard-grid { grid-template-columns: 1fr; }
  .student-list-grid { grid-template-columns: 1fr; }
  .app-container { padding: 100px 20px 40px; }
}
>>>>>>> Stashed changes
